config.py.sample
----------------
    Configuration file sample copy it to config.py and edit to meet yout needs.

fake_kanso/config.py.sample
---------------------------
    Configuration file for fake kanso. The same thing as config.py.sample

log.conf.sample
---------------
    Sample logging configuration. Copy to log.conf and edit.

createIndex.py
--------------
	Script to create SQLite index DB and recreate current.

test_fake_kanso_run.py
----------------------
    Runs fake_kaso

idxd.py
-------
    Runs search server and update server for index.

idxd.log
--------
    Default log file defined by log.conf.
